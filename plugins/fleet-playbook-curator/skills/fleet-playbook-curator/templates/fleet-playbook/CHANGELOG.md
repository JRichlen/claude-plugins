# Fleet playbook changelog

This is the invalidation log: one entry per curation, newest last. A gap in the
dates means the daily loop stopped — if the newest entry is old, the playbook is
stale and should not be trusted. Detector heartbeats ("no change; fleet re-verified")
keep this fresh even when nothing meaningful changed.

<!-- entries appended below by the detector (heartbeats) and curator (real changes) -->
